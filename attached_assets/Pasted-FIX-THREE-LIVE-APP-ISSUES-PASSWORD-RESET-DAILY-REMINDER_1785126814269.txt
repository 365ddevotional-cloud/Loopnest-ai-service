FIX THREE LIVE APP ISSUES — PASSWORD RESET, DAILY REMINDER ENABLING, AND HEADER ORDER

These are focused corrections only.

Do NOT rebuild, redesign, refactor, or inspect unrelated parts of the application.
Do NOT modify Church Mode, Group Mode, Family Mode, Offline Mode, donations, payments, Sunday School, sermon notes, or Android publishing.
Make only the minimum changes required for the three issues below.

==================================================
ISSUE 1 — PASSWORD RESET EMAIL IS NOT BEING SENT
==================================================

LIVE OBSERVATION:

The account email is:

smefs37@gmail.com

The app says that this email already has an account.

When “Forgot Password” is used with this email, the page displays:

“Could not send reset email. Please try again.”

No password-reset email arrives in Inbox, Updates, Promotions, Spam, or Junk.

REQUIRED INVESTIGATION:

1. Trace the complete existing password-reset flow from the Forgot Password form through Firebase Authentication.

2. Capture and inspect the actual Firebase error code and message rather than hiding it behind a generic message.

3. Check all likely causes, including:

- whether Firebase Email/Password authentication is enabled;
- whether sendPasswordResetEmail() is being called correctly;
- whether the correct Firebase project/configuration is being used;
- whether the production domain is listed in Firebase Authentication authorized domains;
- whether actionCodeSettings or continue URLs are invalid;
- whether the reset-email template is enabled and properly configured;
- whether the user exists in the same Firebase Authentication project used by the live app;
- whether Firebase is rejecting the request because of rate limiting, malformed email, disabled user, unauthorized domain, invalid API key, or another specific error;
- whether the app is accidentally suppressing, replacing, or swallowing the true Firebase error.

4. Fix the actual root cause. Do not merely change the visible error message.

5. Keep account-enumeration protection:
   - Do not publicly reveal whether an arbitrary email address is registered.
   - A successful submission may display a safe message such as:

“If an account exists for this email, a password-reset link has been sent. Please also check your Spam, Junk, Promotions, or Updates folder.”

6. Ensure the submit button:
   - shows a loading state only while the request is genuinely pending;
   - becomes usable again after success or failure;
   - cannot remain permanently stuck;
   - prevents accidental repeated submissions.

7. Use the app name in the reset experience wherever Firebase permits:

365 Daily Devotional

8. Do not replace Firebase password reset with SendGrid unless the existing Firebase reset system genuinely cannot work and you first explain why. Firebase should continue securely generating and validating the password-reset link.

LIVE TEST REQUIRED:

Using the existing account email:

smefs37@gmail.com

submit a real password-reset request from the running application.

Confirm from the runtime/browser logs that Firebase accepted the request successfully.

Do not claim the email was delivered merely because the button was clicked. Report:

- the exact Firebase result;
- whether the request was accepted;
- whether delivery to the external Gmail inbox must still be confirmed manually by the account owner.

Never display or log passwords, Firebase tokens, private keys, API keys, or sensitive credentials.

==================================================
ISSUE 2 — DAILY DEVOTIONAL REMINDER STAYS ON “ENABLING…”
==================================================

LIVE OBSERVATION:

The “Daily Devotional Reminders” prompt appears in the lower-right corner.

After clicking Enable/Allow, it remains on:

“Enabling…”

and never completes, succeeds, fails, or returns to a usable state.

REQUIRED INVESTIGATION:

Trace the complete reminder-enablement flow, including:

- browser Notification permission;
- service-worker registration;
- push-manager availability;
- Firebase Cloud Messaging or the existing push provider;
- VAPID/public push key availability;
- push-subscription creation;
- API request that saves the subscription;
- authentication requirements;
- production-domain restrictions;
- iPhone/Safari/PWA limitations;
- desktop Edge/Chrome behavior;
- error handling and loading-state cleanup.

Check the browser console, network requests, service-worker state, server logs, and the actual promise chain.

REQUIRED FIX:

1. The interface must never remain indefinitely on “Enabling…”

2. Add a safe timeout and proper finally-state cleanup.

3. Handle all outcomes clearly:

SUCCESS:
“Daily reminders are enabled.”

PERMISSION DENIED:
“Notifications are blocked. Please allow notifications in your browser or device settings.”

UNSUPPORTED BROWSER:
“Notifications are not supported in this browser.”

APP INSTALL REQUIRED:
When applicable on iPhone/iPad:
“To receive reminders on iPhone or iPad, add 365 Daily Devotional to your Home Screen and allow notifications.”

CONFIGURATION FAILURE:
“We could not enable reminders right now. Please try again.”

4. If notification permission is already:
- granted: continue directly to subscription;
- denied: do not repeatedly request it;
- default: request permission only after the user taps Enable.

5. Ensure the reminder card can be dismissed and does not block other controls.

6. Prevent duplicate subscriptions for the same browser/device.

7. Do not mark reminders enabled until:
- permission is granted;
- a valid push subscription exists;
- and the server has successfully saved or confirmed it.

8. If required configuration is missing, identify it precisely, such as:
- missing VAPID key;
- missing Firebase messaging configuration;
- missing service-worker file;
- failed subscription endpoint;
- unauthorized production origin.

Do not conceal a missing configuration behind a generic success message.

LIVE TEST REQUIRED:

Test in the running application using a supported desktop browser such as Edge or Chrome:

A. Permission granted
- Click Enable.
- Confirm it leaves “Enabling…”
- Confirm a valid subscription is created or reused.
- Confirm the server accepts it.
- Confirm the success state appears.

B. Permission denied
- Confirm it displays a clear blocked-permission message.
- Confirm the button does not remain stuck.

C. Simulated request/server failure
- Confirm the UI exits loading and shows a recoverable error.

D. Reload the page
- Confirm an already-enabled device is recognized correctly and is not subscribed twice.

Do not claim that an actual scheduled push notification was received unless one was truly sent and observed. Clearly distinguish:
- subscription successfully enabled;
- actual reminder delivery still requiring a scheduled/live delivery test.

==================================================
ISSUE 3 — MOVE SIGN IN TO THE BEGINNING OF THE HEADER
==================================================

CURRENT DESKTOP ORDER:

Daily Devotional logo/name
→ How to Use
→ Today
→ Archive
→ other navigation items
→ Church Mode
→ Login
→ Sign In
→ Bible selector

REQUIRED ORDER:

Daily Devotional logo/name
→ Sign In
→ How to Use
→ Today
→ Archive
→ remaining existing items in their present order

REQUIREMENTS:

1. Move the existing “Sign In” navigation item so it is the first navigation item immediately after the Daily Devotional logo/name.

2. Do not create a duplicate Sign In item.

3. Remove the old Sign In position at the far-right side.

4. Preserve its existing route and behavior.

5. If the user is already authenticated, keep the current signed-in account/profile behavior in that same first navigation position.

6. Do not alter the Bible selector or the order of any other menu items except as required to move Sign In.

7. Ensure:
- desktop navigation works;
- mobile hamburger navigation still works;
- there is no overflow, overlap, or wrapping caused by the change;
- keyboard focus order follows the visual order;
- screen-reader labels remain correct.

==================================================
TESTING AND FINAL REPORT
==================================================

Test all three corrections in the running application before declaring completion.

FINAL REPORT MUST INCLUDE:

1. PASSWORD RESET
- exact root cause;
- exact Firebase error code previously occurring;
- files changed;
- confirmation that a real reset request for smefs37@gmail.com was accepted by Firebase;
- whether external inbox delivery still requires manual confirmation.

2. DAILY REMINDERS
- exact reason the UI was stuck on “Enabling…”;
- exact fix;
- service-worker status;
- notification permission result;
- push-subscription result;
- server-save result;
- supported-browser test result;
- anything that still requires physical iPhone/Android testing.

3. HEADER
- old order;
- new order;
- confirmation there is only one Sign In item;
- desktop and mobile results.

4. GENERAL
- browser console results;
- server-log results;
- TypeScript/build results limited to affected files;
- exact files changed;
- confirmation no unrelated files were modified.

If any required secret, Firebase Console setting, authorized-domain change, VAPID key, or manual external configuration is needed, do not guess or pretend it is complete. State the exact setting, exact location, and exact action the app owner must perform.

Create a checkpoint only after the corrections have been implemented and the available tests have passed.